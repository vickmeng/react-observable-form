import React, { useRef } from "react";

// import { GroupControl } from "../../../packages/controls/groupControl";

const Group = () => {
  // const groupRef = useRef(new GroupControl({}));

  return <>1</>;
};

export default Group;
